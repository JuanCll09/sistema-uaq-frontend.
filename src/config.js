// --- FASE FINAL: CORRECCIÓN DE MIXED CONTENT ---
    // Usamos el protocolo HTTPS para evitar el bloqueo del navegador.
    // Esto funciona porque la llamada inicial de verificación es bloqueada por el navegador.
    const API_BASE_URL = 'https://18.221.215.255/proyecto_uaq'; 

    export default API_BASE_URL;
